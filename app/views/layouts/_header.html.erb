<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="/">Money Spent Counter </a>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="<%= root_path %>">Invoices</a>
      </li>
    </ul>
    <% if user_signed_in? %>
    <%= link_to '✏️ Edit profile',
            edit_user_registration_path,
            title: "Click to edit " + current_user.email[/[^@]+/] + " profile",
            class: "btn btn-success m-2 my-sm-0"%>
    <%= link_to "⏏️ Logout " + current_user.email[/[^@]+/],
            destroy_user_session_path, method: :delete,
            title: "Click to logout",
            class: "btn btn-success my-2 my-sm-0" %>
    <% else %>
    <div>
      <%= link_to "Sign up", new_user_registration_path,
            class: "btn btn-success my-sm-0 m-2" %>
      <%= link_to "Login", new_user_session_path,
            class: "btn btn-success my-2 my-sm-0" %>
      <% end %>
    </div>
</nav>
